// Relatives
import Typed from './Typed';
import Settings from './Settings';

Typed.type = 'typed';
Typed.pluginSettings = Settings;
Typed.version = VERSION;

export const plugins = {};

export const version = VERSION;

export default Typed;
